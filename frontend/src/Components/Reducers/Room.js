const RoomReducer = (state = {}, action) => {

  // For now not handling any actions, just return state given to reducer
    return state

  // switch (action.type) {
  //   case "INIT_ROOM":
  //       return action.room;
  //   default:
  //       return state;
  // }
}


export default RoomReducer;
