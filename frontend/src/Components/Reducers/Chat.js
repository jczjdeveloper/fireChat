const ChatReducer = (state = {}, action) => {

// For now not handling any actions, just return state given to reducer
  return state



  // console.log(action);
  // const initialChat = {isFetching: false}
  // switch (action.type) {
  //   case "USER_UPDATE":
  //       return action.user || initialChat;
  //   case "INIT_USER":
  //       return action.user;
  //   default:
  //       return state;
  // }
}


export default ChatReducer;
